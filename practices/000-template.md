# <Practice name>

**Bias:** <The team's standing voice, spoken to the agent: "You are the team's senior
<profession>: <the two or three instincts that actually change decisions>." Sinapsi injects
this paragraph on every task start and on every message — it is the sentence the developer
would otherwise retype into every prompt. Keep it sharp enough to change a choice.>

**When:** <the condition under which the full file below is worth reading — one line>

<!-- One practice per file. This folder is the team's: Sinapsi seeds it once and never
     writes here again — edit, rename, delete freely; nothing is restored, no index is
     generated from these files, and no document points at them by name. Only the
     **Bias:** paragraph is injected; everything below it is the deep version, read when
     the When matches. A change here is in force from the next message, nothing to run. -->

## Do

<The moves that follow from that stance: what to read first, what to measure, what to
check before calling it done. Keep them checkable.>

## Never

<The single failure this practice exists to prevent, named plainly.>
