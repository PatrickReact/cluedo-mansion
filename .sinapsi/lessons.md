# Lessons

One line per thing this project already learned, so nobody learns it twice. Consult it
before re-opening a settled choice or debugging something familiar; append, never rewrite.
A big, irreversible architectural decision goes in `adr/` instead.

## Decisions — the reasoning that must survive, not the diff

`- YYYY-MM-DD: chose X over Y because Z`

## Pitfalls — what bit us, and how not to be bitten again

`- symptom → root cause → fix / how to avoid`
